syntax = "proto3";

package api.player.intra.v1;

import "errors/errors.proto";

option go_package = "api/player/intra/v1;intrav1";

enum TunnelServiceErrorReason {
  option (errors.default_code) = 500;

  SERVER_ERROR = 0 [(errors.code) = 500];
  UID_ERROR = 1 [(errors.code) = 401];
}
